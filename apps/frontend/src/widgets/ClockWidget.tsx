import type { WidgetViewProps } from './types';

export default function ClockWidget(_props: WidgetViewProps) {
  return (
    <div className="rounded-xl border border-white/10 bg-[var(--surface)] p-4 text-[var(--muted)] text-lg">
      Clock widget (coming soon)
    </div>
  );
}
