/**
 * Diagnostic: when `true`, display clients always see full initiative (no masking / “reveal lowest” behavior),
 * and the DM Settings checkboxes for this feature are hidden.
 *
 * **`true`** = run the experiment (privacy off + settings hidden). **`false`** = normal product behavior.
 */
export const DEBUG_DISABLE_DISPLAY_INITIATIVE_PRIVACY = false;
